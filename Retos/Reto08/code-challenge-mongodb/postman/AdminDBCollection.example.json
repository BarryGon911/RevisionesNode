{
  "info": {
    "_postman_id": "e6e3b9a0-5f56-4e62-9c0b-111111111111",
    "name": "Code Challenge MongoDB — Admin DB",
    "description": "Endpoints de administración para dropear/seedear/resetear la base (solo dev). Si defines ADMIN_KEY en .env, debes enviar el header x-admin-key con el mismo valor.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:3000/...." },
    { "key": "adminKey", "value": "" }
  ],
  "item": [
    {
      "name": "POST /admin/db/drop",
      "request": {
        "method": "POST",
        "header": [
          { "key": "x-admin-key", "value": "{{adminKey}}", "type": "text" }
        ],
        "url": {
          "raw": "{{baseUrl}}/admin/db/drop",
          "host": ["{{baseUrl}}"],
          "path": ["admin", "db", "drop"]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('Status 200', () => pm.response.code === 200);",
              "let b={}; try{b=pm.response.json()}catch(e){}",
              "pm.test('ok=true', () => b && b.ok === true);"
            ]
          }
        }
      ]
    },
    {
      "name": "POST /admin/db/seed",
      "request": {
        "method": "POST",
        "header": [
          { "key": "x-admin-key", "value": "{{adminKey}}", "type": "text" }
        ],
        "url": {
          "raw": "{{baseUrl}}/admin/db/seed",
          "host": ["{{baseUrl}}"],
          "path": ["admin", "db", "seed"]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('Status 200', () => pm.response.code === 200);",
              "let b={}; try{b=pm.response.json()}catch(e){}",
              "pm.test('ok=true', () => b && b.ok === true);"
            ]
          }
        }
      ]
    },
    {
      "name": "POST /admin/db/reset",
      "request": {
        "method": "POST",
        "header": [
          { "key": "x-admin-key", "value": "{{adminKey}}", "type": "text" }
        ],
        "url": {
          "raw": "{{baseUrl}}/admin/db/reset",
          "host": ["{{baseUrl}}"],
          "path": ["admin", "db", "reset"]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('Status 200', () => pm.response.code === 200);",
              "let b={}; try{b=pm.response.json()}catch(e){}",
              "pm.test('ok=true', () => b && b.ok === true);"
            ]
          }
        }
      ]
    }
  ]
}