# Base de datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=BarryGon911MySQL2026LaQuerencia30420
DB_NAME=biblioteca
DB_PORT=3306
DB_DIALECT=mysql
DB_LOGGING=false

# Opcional: zona horaria usada por Sequelize al escribir/leer fechas
# Formato: '+00:00', '-06:00', 'Z' (UTC). Para CDMX:
DB_TIMEZONE=-06:00

# Alternativa (opcional): cadena de conexión completa
# DATABASE_URL=mysql://root:changeme@localhost:3306/biblioteca

# NodeJS Server
SRV_PORT=3000